const routeConfig = {
  home: "/",
  auth: "/auth",
  doctor: "/doctor",
  schedule: "/schedule",
  history: "/history",
  forum: "/forum",
  about: "/about",
  profile: "/profile",
  changePassword: "/change-password",
  payment: "/payment",
  default: "/*"
};

export default routeConfig;
